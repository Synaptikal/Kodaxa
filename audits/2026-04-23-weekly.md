---
TRANSMISSION · KODAXA INTERNAL SYSTEMS
REF: AUDIT-2026-04-23
Filed: 2026-04-23 · Closed: 2026-04-23
Division: Internal Systems
Classification: Internal
---

# Weekly Audit — April 23, 2026

## Auto-Fixed (this run)

### Security

**[SEC-01] Middleware auth gap — FIXED**
`src/lib/supabase/proxy.ts` now captures the user from `getUser()` and redirects
unauthenticated requests to `/auth/sign-in?next={pathname}` for both `/corp/hq` and
`/admin` prefixes. `/terminal` is intentionally excluded — it renders a public sign-in
CTA by design. Component-level guards remain as the real security boundary per
CVE-2025-29927 (Next.js middleware bypass; patched in ≥ 15.2.3).

**[SEC-02] JSON-LD XSS — FIXED**
Added `.replace(/</g, '\\u003c')` to every `dangerouslySetInnerHTML` JSON-LD block
that touches user-supplied data. This prevents `</script>` injection through
`JSON.stringify()` output. `\u003c` is valid JSON per RFC 8259 §7.
- `src/app/directory/[in_game_name]/page.tsx` — ld, breadcrumbLd, reviewsLd
- `src/app/dispatch/[slug]/page.tsx` — ld, breadcrumbLd
- `src/app/layout.tsx` — WEBAPP_LD (static data; consistent treatment)
- `src/app/makers/[mark]/page.tsx` — maker profile LD

### Code Health — File Size

**[CODE-05] Warning-zone files split or evaluated**

`src/data/patch-notes/` — split 578-line monolith into 6 modules:
- `types.ts` (23 lines) — type definitions
- `2026.ts` (126 lines) — 2026 entries
- `2025-late.ts` (228 lines) — Sep–Dec 2025 entries
- `2025.ts` (186 lines) — Feb–Aug 2025 entries
- `legacy.ts` (28 lines) — Alpha/2024 entries
- `index.ts` (29 lines) — barrel re-export + merged `PATCH_NOTES` array

`src/data/items/crafted.ts` — split 370-line file into 3 modules:
- `crafted.ts` (14 lines) — barrel re-export only; public API unchanged
- `crafted-tools.ts` (258 lines) — TOOL_ITEMS (24 Toolmaker station tools)
- `crafted-consumables.ts` (123 lines) — FOOD_ITEMS + MEDICINE_ITEMS (Stove station)

`src/app/page.tsx` (352 → 291 lines) — extracted DIVISIONS, PHASE_COLORS,
and TICKER_MESSAGES into `src/app/landing-config.ts`. Icons (Crosshair, Database,
Scale, Radio) and DivisionConfig type moved with them. landing-config.ts grew
from 154 → 236 lines. Both files well under 400.

**Not split — rationale documented:**

`src/app/system/page.tsx` (379 lines) — pure declarative JSX with zero business
logic. The file's single concern is the complete design system reference. Splitting
10 `<Section>` blocks into files adds 10+ modules with no logic-separation benefit.
The sub-components (`Section`, `Swatch`, `ContractRow`, `VocabRow`) already live in
`./_components`. Acceptable at 379 lines; monitor at next active development pass.

`src/data/building/tiles.ts` (365 lines) — single homogeneous `FABRICATOR_TILES`
array. All entries share the same `TileDef` type and interface. Subcategory groupings
(structural, walls, access, decor, lighting, supports) are comments inside one array,
not separate logical concerns. Splitting by subcategory would create arbitrary
fragmentation. Acceptable at 365 lines; add `tiles-decor.ts` split only if the
array exceeds 450 lines during future content updates.

### Code Health — Loading States

**[CODE-02] + [CODE-03] Loading states — FIXED**
Created 37 route-segment `loading.tsx` files using a consistent SR-themed template
(`bg-sr-bg`, `text-sr-muted`, `font-mono`, `animate-pulse`, uppercase tracking).
Updated root `src/app/loading.tsx` from shadcn token (`text-muted-foreground`) to
SR token (`text-sr-muted`).

Routes covered: terminal, dashboard, corp/hq (+ all sub-routes: commissions, dispatch,
dispatch/new, dispatch/[slug]/edit, roster, skills, supply), atlas, market, directory
(+ me + [in_game_name]), planner, inventory, admin/feedback, auth/sign-in, auth/sign-up,
biome-guide, building, corp/join, corporation, crafting, creatures, dispatch (+ [slug]),
feedback, items, makers (+ [mark]), patch-notes, recipes, system, trade, xp-timer.

### Design System

**[DESIGN-01] Reduced-motion — FIXED**
Added `@media (prefers-reduced-motion: reduce)` block at end of `globals.css`
covering `.starfield::before/after`, `.status-dot-live`, `.relay-ticker-track`,
and `.skeleton-row`. Used override approach (not no-motion-first restructuring)
per code-warden patch-first rule. WCAG 2.1 SC 2.2.2 compliant.

Also removed dead code: `@keyframes star-twinkle` was defined in `globals.css`
but never applied via CSS. The twinkle effect runs in JavaScript canvas inside
`star-field.tsx`. Keyframe definition deleted.

**[DESIGN-02] SVG hardcoded colours — FIXED**
Replaced 3 hardcoded `rgb()` values in `klaatu-flow-chart.tsx` with CSS custom
properties via `style` prop (SVG presentation attributes cannot resolve CSS vars;
the `style` prop is in CSS parsing context and can use `var()`):
- Earned bar: `style={{ fill: 'var(--color-brand-emerald)' }}` (was `rgb(52,211,153)`)
- Spent bar:  `style={{ fill: 'var(--color-cat-combat)' }}`  (was `rgb(248,113,113)`)
- Baseline:   `style={{ stroke: 'var(--color-sr-subtle)' }}` (was `rgb(51,65,85)`)

### API

**[API-01] Error response shape — FIXED**
Added `apiError(message, code, status)` helper to `src/app/api/feedback/route.ts`.
All error responses now return `{ error: string, code: string }`. Error codes:
`DB_INSERT_FAILED`, `DB_FETCH_FAILED`, `DB_DELETE_FAILED`, `DB_UPDATE_FAILED`,
`MISSING_ID`, `MISSING_FIELDS`, `INVALID_BODY`.

---

## Open — No Action Required This Run

### 🟡 Monitoring

**[SEC-03] CSP: `'unsafe-eval'` and `'unsafe-inline'` on `script-src`**
Both are acknowledged in `next.config.ts` comments (Three.js/R3F shader compilation +
Next.js hydration). Known trade-offs. Evaluate nonce-based CSP when upgrading to
Next.js 16 or when Three.js usage is isolated. No action now.

**[CODE-01] `'use client'` after JSDoc blocks**
Per Next.js App Router spec, comments before `'use client'` are acceptable.
Confirmed not an error. Run `next lint` to verify ESLint config agrees.

**[CODE-04] Inline `style={{}}` in 17 locations**
Most instances are for dynamic values (progress bar widths, calc expressions,
dynamic colours). Acceptable. Cosmetic candidates for cleanup: `footer.tsx:25`,
`boot-sequence.tsx:90` — low priority.

**[CODE-05] Remaining monitoring-zone files (300–399 lines)**
All below the 400-line warning threshold. Evaluate if any grows during feature work:
- `src/components/ui/nav-header.tsx` — 326 lines (types + config + sub-components
  co-located; all private to this file; acceptable)
- `src/app/corporation/page.tsx` — 318 lines
- `src/components/corp/supply-board.tsx` — 317 lines
- `src/components/market/price-report-form.tsx` — 309 lines

**[COPY-01] `system/page.tsx` vocabulary table**
"Sign Up" and "Submit" appear in the *generic* column of `<VocabRow>` — documentation
of wrong copy, not live UI strings. No change needed.

**[DATA-01] Hardcoded baseline counts**
No stale hardcoded counts found in UI strings. Professions/skills data drives counts
dynamically. Stations confirmed: 4 (Lathe, Stove, Toolmaker, Refinery).

---

## Systems Status — Final

| Section | Status |
|---|---|
| Security — Headers | ✅ PASS — all 5 headers; poweredByHeader: false |
| Security — Route Guards | ✅ PASS — middleware guards /corp/hq + /admin; component guards remain real boundary |
| Security — NEXT_PUBLIC_ secrets | ✅ PASS — only Supabase URL + publishable key exposed (both public-safe) |
| Security — JSON-LD XSS | ✅ PASS — .replace(/</g, '\\u003c') applied to all user-data LD blocks |
| Security — CSP | ⚠️ MONITOR — unsafe-eval/inline acknowledged trade-offs; revisit at Next.js 16 |
| Code — File Size | ✅ PASS — no files over 400 lines; 4 files in 300–399 monitoring zone |
| Code — Loading States | ✅ PASS — 37 route-segment loading.tsx files created; root updated |
| Code — console.log | ✅ PASS — zero in source |
| Code — Class components | ✅ PASS — none |
| Code — Bare img | ✅ PASS — none |
| Code — use client placement | ✅ PASS — all correct per spec |
| Code — API response shapes | ✅ PASS — apiError helper enforces { error, code } |
| Design — Accessibility / Motion | ✅ PASS — prefers-reduced-motion override added; dead keyframe removed |
| Design — Design Tokens | ✅ PASS — SVG hardcoded colors replaced with CSS custom properties |
| Design System Copy | ✅ PASS — no live UI using forbidden generic copy |
| Data Integrity | ⚠️ MONITOR — counts not re-verified against live game state this run |

---

## Session Notes

- CVE-2025-29927 (Next.js middleware bypass, Mar 2025) is the reason middleware
  alone cannot be the sole auth boundary. Component-level guards are the real
  security line; middleware acts as a UX-level redirect only.
- The `/terminal` route intentionally shows a public sign-in CTA rather than
  redirecting — confirmed design decision, not a bug.
- `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` naming is the new Supabase SSR convention;
  not a misconfiguration.
- No Framer Motion in codebase — all animations are CSS-native. Remove Framer
  Motion check from future audit templates.
- `star-twinkle` keyframe in globals.css confirmed unused via grep:
  only references are JavaScript property names in star-field.tsx canvas code.
